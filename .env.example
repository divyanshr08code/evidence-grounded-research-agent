# No environment variables are required yet.
#
# The LLM provider, vector store, and other external services this agent
# will depend on have not been decided yet. As those decisions are made,
# add the corresponding variable names here (never real values/secrets).
